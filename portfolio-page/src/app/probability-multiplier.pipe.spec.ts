import { ProbabilityMultiplierPipe } from './probability-multiplier.pipe';

describe('ProbabilityMultiplierPipe', () => {
  it('create an instance', () => {
    const pipe = new ProbabilityMultiplierPipe();
    expect(pipe).toBeTruthy();
  });
});
